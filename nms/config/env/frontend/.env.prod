# Frontend app build env (production)
# Option A: /api via frontend worker proxy
# Option B: full backend URL, e.g. https://backend.example.com/api
REACT_APP_API_BASE_URL=/api
REACT_APP_SCOPES=openid profile email offline_access User.Read
