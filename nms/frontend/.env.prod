# Frontend app production env (used by ./build.sh prod)
# Use /api when frontend Worker proxies to backend Worker.
# For direct backend calls set full URL: https://backend.example.com/api
REACT_APP_API_BASE_URL=/api
REACT_APP_SCOPES=openid profile email offline_access User.Read
