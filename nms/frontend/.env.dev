# Frontend app development env (used by ./build.sh dev)
# Keep /api when frontend Worker proxies API requests to backend Worker.
REACT_APP_API_BASE_URL=/api
REACT_APP_SCOPES=openid profile email offline_access User.Read
